@model IEnumerable<Caja.Context.EQUIVALENCIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEquivalencia }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEquivalencia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEquivalencia })
        </td>
    </tr>
}

</table>
